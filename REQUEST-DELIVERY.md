# Request delivery

GameVerse keeps the request delivery destination internal to the application interface.

Submitting the form opens the user's configured email app with the request details pre-filled. The user completes the final Send action in their email app. The public interface does not display where requests are routed.
