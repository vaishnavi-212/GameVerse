# GameVerse

**Play what the moment feels like.**

A premium offline-first mini-game collection redesigned with a playful editorial visual system. Each game keeps its original gameplay logic while receiving a dedicated monochrome visual theme and custom local artwork.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
